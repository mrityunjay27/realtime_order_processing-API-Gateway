# Public routes exposed by the gateway.
#
# Every route is a thin pass-through to a client. The gateway never
# contains business logic: order creation rules, inventory rules, and
# payment rules stay in their own services.
